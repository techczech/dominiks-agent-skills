## What this repo is

A {{SKILL_KIND}} skill. The skill itself is defined in `SKILL.md` (YAML frontmatter + markdown body). This `AGENTS.md` is for agents working *on* the skill, not agents *using* it.

## Key files

- `SKILL.md` — skill spec; YAML frontmatter (name, description) + markdown body
- `references/` — support docs; keep individual files < 300 lines
- `assets/` — templates, fonts, icons used in skill output
- `scripts/` — executable helpers the skill invokes

## Commands

{{COMMANDS}}

## Verification

{{VERIFICATION}}

## Sibling skills

{{SIBLING_SKILLS}}

## Dependencies

{{DEPENDENCIES}}
